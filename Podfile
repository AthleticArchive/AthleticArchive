# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AthleticArchive' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AthleticArchive

  target 'AthleticArchiveTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AthleticArchiveUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
